# must be unique in a given SonarQube instance
sonar.projectKey=empresa:sistemaweb

# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=SistemaWeb
sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# Since SonarQube 4.2, this property is optional if sonar.modules is set.
# If not set, SonarQube starts looking for source code from the directory containing
# the sonar-project.properties file.
sonar.sources=.
sonar.exclusions=/static/bower_components/**,/static/compress/**,/data/**,/conf/**,/logs/**,/test/frontend/**

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8

#sonar.tests=test/unit/backend/**
#sonar.python.xunit.skipDetails=false
#/test/report/backend_unit_report/xunittest.xml #xunittest.xml
#/test/report/backend_unit_report/coverage.xml
sonar.python.xunit.reportPath= xunittest.xml
sonar.python.coverage.reportPath=coverage.xml
sonar.python.xunit.skipDetails=false
